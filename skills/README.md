# Skills Index

- `validate-spec-suite` — Validate OpenClaw specs and contracts (JSON Schema + doc links + examples).
- `spec-ref-audit` — Audit spec references ($ref, Markdown links, path mentions).
- `detect-breaking-changes` — Compare baseline vs current branch to identify breaking changes and migration steps.
